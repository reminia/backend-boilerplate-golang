{
  "name": "backend-boilerplate",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && go build",
    "start": "./app/server",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^9.7.0"
  }
}
