{
  "name": "backend-boilerplate",
  "version": "0.0.1",
  "scripts": {
    "build": "cd app && go build server.go",
    "start": "./app/server",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^5.4.0"
  }
}
